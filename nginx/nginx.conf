http {

#   upstream client {
#   server client:300;
# }

upstream registry {
  server registry:5000;
}

server {
  listen 80;

  location / {
    proxy_pass http://client;
  }

  

   
   location /registry {
    rewrite /registry/(.*) /$1 break;
    proxy_pass http://registry;
 
  }

}
}
events{}